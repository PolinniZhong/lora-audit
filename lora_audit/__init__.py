# -*- coding: utf-8 -*-
"""lora-audit —— LoRA 训练资产的只读审计视图。

设计边界（有意为之）：
  - **零必需依赖**：只用 Python 标准库。审计只需要读文件、跑正则、写 HTML。
  - **不读取任何"本项目的契约"**：输入就是一个普通文件夹，不需要 manifest、
    不需要声明文件、不需要数据库。用户不该为了看图改自己的目录结构。
  - **构建期一次性出静态页**：不起服务、不开端口、不建索引。
  - **只读**：不移动、不改名、不改写数据集里的任何文件。
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
